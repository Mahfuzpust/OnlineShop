@model IEnumerable<OnlineShop.Models.ProductTypes>
@{
    ViewData["Title"] = "Index";
}


<div class="container">
    <div class="row">
        <div class="col-8 offset-2">
            <h1 class="text-center text-primary">Product Type List</h1>
            <a class="btn btn-sm btn-primary m-2" asp-controller="ProductTypes" asp-action="Create"> <i class="fa-sharp fa-solid fa-plus"></i>Add New Product</a>
                <table class="table table-success">
                    <tr class="text-center">
                        <th>Id</th>
                        <th>ProductType</th>
                        <th></th>
                        <th>Action</th>
                    </tr>
                    @foreach (var data in Model)
                    {
                        <tr class="text-center">
                            <td>@data.Id</td>
                            <td>@data.ProductType</td>
                            <td>
                            <partial name="_ButtonPartial" model="@data.Id" />
                            </td>
                        </tr>
                    }
                </table>
        </div>
    </div>
</div>

@*<div class="container">
    <div class="row">
        <div class="col-8 offset-2">
            <h1 class="text-center text-primary">ProductType list</h1>
                <a class="btn btn-sm btn-primary m-2" asp-controller="ProductTypes" asp-action="Create">Create Product Type</a>
                <table class="table table-dark">
                    <tr class="text-center">
                        <th>@Html.DisplayNameFor(e=>e.ProductType)</th>
                        <th>Action</th>
                    </tr>
                    @foreach (var data in Model)
                    {
                        <tr class="text-center">
                            <td>@data.ProductType</td>
                            <td>Edit | Details | Delete</td>
                        </tr>
                    }
                </table>
        </div>
    </div>
</div>*@



